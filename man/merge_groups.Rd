% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/groups.R
\name{merge_groups}
\alias{merge_groups}
\title{Merge sparse groups}
\usage{
merge_groups(groups, grp_formula = NULL, ..., min_cell_size = 50)
}
\arguments{
\item{groups}{An \code{idifr_groups} object from \code{check_groups()}, or a data
frame (in which case \code{grp_formula} must also be supplied).}

\item{grp_formula}{A formula, required only if \code{groups} is a raw data frame.}

\item{...}{Named arguments specifying merge rules. Each should be named
after a demographic variable, with a named list mapping new level names
to vectors of old level names.}

\item{min_cell_size}{Minimum cell size to validate against after merging.}
}
\value{
The original data frame with recoded grouping variable(s).
}
\description{
Combines sparse intersectional cells by collapsing levels of one or more
demographic variables. Returns a modified data frame ready to pass back
to \code{idifr()} or \code{check_groups()}.
}
\examples{
\dontrun{
grp <- check_groups(my_data, group = ~ nationality * age_band)

merged_data <- merge_groups(
  grp,
  age_band = list("18-30" = c("18-24", "25-30"))
)
}

}
