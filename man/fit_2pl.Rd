% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irt_2pl.R
\name{fit_2pl}
\alias{fit_2pl}
\title{Fit a 2PL IRT model via marginal maximum likelihood (EM)}
\usage{
fit_2pl(
  resp,
  group = NULL,
  constrain = "items",
  n_nodes = 15,
  max_iter = 200,
  tol = 1e-04,
  start = NULL,
  verbose = FALSE
)
}
\arguments{
\item{resp}{Integer matrix (0/1/NA). Rows=persons, cols=items.}

\item{group}{Character/factor vector of group membership (length=nrow(resp)).
NULL for single-group calibration.}

\item{constrain}{Parameter constraint across groups:
\itemize{
\item \code{"items"} — a and b equal across groups (DIF null hypothesis)
\item \code{"none"}  — all parameters free (separate group calibrations)
\item \code{"alpha"} — a fixed across groups, b free (uniform DIF test)
\item \code{"beta"}  — b fixed across groups, a free (non-uniform DIF test)
}}

\item{n_nodes}{Number of quadrature nodes. Default 15. Values of 11-21
are appropriate for DIF detection; use 21 for publication-
quality parameter estimates.}

\item{max_iter}{Maximum EM iterations. Default 200.}

\item{tol}{Convergence tolerance on log-likelihood change. Default 1e-4.}

\item{start}{Optional list with elements \code{a} and \code{b} (numeric
vectors of length = number of items) to warm-start the EM
loop. If \code{NULL} (default), the usual data-driven
starting values are used.}

\item{verbose}{Print iteration log. Default FALSE.}
}
\value{
Object of class \code{irt_2pl}.
}
\description{
Fit a 2PL IRT model via marginal maximum likelihood (EM)
}
