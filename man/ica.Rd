% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ica.R
\name{ica}
\alias{ica}
\title{Intersectional Contrast Analysis (ICA)}
\usage{
ica(
  data,
  items,
  group,
  method,
  min_cell_size = 50,
  alpha = 0.05,
  p_adjust = "BH",
  effect_threshold = 0.035,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame containing item responses and demographic variables.}

\item{items}{Numeric or character vector identifying the item columns.}

\item{group}{A one-sided formula with \strong{at least two} demographic variables,
e.g. \code{~ gender * nationality * age_band}. ICA is not meaningful for a
single-variable design — use \code{\link[=idifr]{idifr()}} directly in that case.}

\item{method}{\code{"LR"} or \code{"LRT"} — the DIF detection method passed to each
\code{idifr()} call.}

\item{min_cell_size}{Minimum acceptable group size passed to \code{idifr()}.
Default 50.}

\item{alpha}{Significance threshold. Default 0.05.}

\item{p_adjust}{P-value adjustment method passed to \code{idifr()}. Default
\code{"BH"}.}

\item{effect_threshold}{Minimum effect size required for an item to be
counted as flagged in a single-variable or intersectional run. For \code{"LR"}
this is the minimum Nagelkerke ΔR²; for \code{"LRT"} the minimum std. chi.
Defaults to \code{0.035} (ETS Class B). A stricter value here partially
compensates for running multiple analyses without cross-analysis
correction.}

\item{verbose}{Logical. Print progress and notes. Default \code{TRUE}.}

\item{...}{Additional arguments passed to each \code{idifr()} call.}
}
\value{
An object of class \code{"idifr_ica"} containing:
\describe{
\item{\code{classification}}{Data frame (one row per item) with columns
\code{item}, \code{classification}, \code{marginal_variable}, \code{intersectional_flagged},
\code{method}.}
\item{\code{single_results}}{Named list of \code{tidy()} data frames, one per
demographic variable.}
\item{\code{intersectional_result}}{\code{tidy()} data frame from the full
intersectional run.}
\item{\code{vars}}{Character vector of demographic variable names.}
\item{\code{single_flags}}{Named list of character vectors: the item names
flagged in each single-variable run (using \code{effect_threshold}).}
\item{\code{method}, \code{alpha}, \code{effect_threshold}}{Parameters used.}
\item{\code{n_groups}}{Number of intersectional groups.}
\item{\code{call}}{The matched call.}
}
}
\description{
Runs a series of \code{idifr()} analyses — one per demographic variable and one
with the full intersectional formula — then classifies each item by
comparing where it was flagged.

ICA distinguishes four patterns:
\describe{
\item{\code{"amplified"}}{Flagged in both a single-variable run \emph{and} the
intersectional run. The intersectional design amplifies the signal.}
\item{\code{"pure_intersection"}}{Flagged only in the intersectional run. DIF
is invisible when examining demographic variables individually.}
\item{\code{"obscured"}}{Flagged in a single-variable run but \emph{not} in the
intersectional run. The intersectional crossing dilutes or reverses the
effect.}
\item{\code{"none"}}{Not flagged in any analysis.}
}
}
\examples{
\dontrun{
library(iDIFr)
set.seed(42)
dat <- simulate_dif(1000, 20, 2, c(3, 7), 1.0, seed = 42)
dat$nationality <- sample(c("UK", "DE", "FR"), 1000, replace = TRUE)
dat$age_band    <- sample(c("Young", "Old"),   1000, replace = TRUE)

ica_res <- ica(
  data   = dat,
  items  = 1:20,
  group  = ~ group * nationality * age_band,
  method = "LR"
)
print(ica_res)
tidy(ica_res)
}

}
\seealso{
\code{\link[=idifr]{idifr()}} for single-run DIF analysis.
}
