% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/idifr.R
\name{idifr}
\alias{idifr}
\title{Run intersectional DIF analysis}
\usage{
idifr(
  data,
  items,
  group,
  method,
  min_cell_size = 50,
  exclude_below_min = FALSE,
  fully_crossed = NULL,
  value_selection = NULL,
  anchor = NULL,
  alpha = 0.05,
  p_adjust = "BH",
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing item responses and demographic variables.}

\item{items}{A numeric vector of column indices, or a character vector of
column names, identifying the item response columns. Items must be
dichotomously scored (0/1).}

\item{group}{A one-sided formula specifying the grouping variable(s).
Use \code{~ var} for a single demographic (2+ groups) or \code{~ var1 * var2} for
intersectional groups. Example: \code{~ gender * nationality * age_band}.}

\item{method}{A character vector specifying which DIF method(s) to use.
Must be one or more of \code{"LR"} (Logistic Regression), \code{"LRT"} (IRT
Likelihood Ratio Test), \code{"ID"} (Intersectional Decomposition), or \code{"RF"}
(Random Forest structural-change test). No default -- the user must
choose.}

\item{min_cell_size}{Minimum acceptable group size. Groups below this
threshold trigger a warning. Also used as the crossing criterion when
\code{exclude_below_min = TRUE} or \code{fully_crossed} is supplied. Default is 50.}

\item{exclude_below_min}{Logical. If \code{TRUE}, any intersectional group with
fewer than \code{min_cell_size} respondents is excluded from the analysis
entirely. If \code{FALSE} (default), all groups are included and small groups
trigger a warning only.}

\item{fully_crossed}{A character vector of variable name(s). Only levels of
the named variable(s) that are fully crossed -- meaning every intersectional
cell for that level meets \code{min_cell_size} -- are included in the analysis.
Respondents belonging to levels that are not fully crossed are excluded.
Default is \code{NULL} (no crossing filter applied). Example:
\code{fully_crossed = "nationality"} keeps only nationalities where every
gender x age_band cell meets \code{min_cell_size}.}

\item{value_selection}{A named list for filtering specific values of
demographic variables before analysis. Each element should be named after
a grouping variable and contain a character vector of values to keep.
Variables not mentioned are left unchanged (all values included). Default
is \code{NULL}. Example: \code{value_selection = list(country = c("UK", "France"), age_band = c("Young", "Old"))}.}

\item{anchor}{A numeric or character vector identifying anchor items
(items assumed to be DIF-free) for IRT scaling. If \code{NULL} (default),
all items are used as anchors in the first pass.}

\item{alpha}{Significance level for DIF flagging. Default is \code{0.05}.}

\item{p_adjust}{Method for p-value adjustment across items. Passed to
\code{\link[stats:p.adjust]{stats::p.adjust()}}. Default is \code{"BH"} (Benjamini-Hochberg). Use
\code{"none"} to skip adjustment.}

\item{verbose}{Logical. If \code{TRUE} (default), prints progress and group
information during the analysis.}
}
\value{
An object of class \code{idifr} containing:
\describe{
\item{results}{A data frame with one row per item per method, including
test statistics, p-values, adjusted p-values, effect sizes, and DIF
classification (A/B/C for LR; negligible/moderate/large for LRT and ID).}
\item{groups}{An \code{idifr_groups} object describing the group structure,
cell sizes, and any small-cell warnings.}
\item{method}{Character vector of methods used.}
\item{call}{The matched call.}
\item{items}{Character vector of item names analysed.}
\item{alpha}{The significance level used.}
\item{p_adjust}{The p-value adjustment method used.}
\item{excluded_groups}{Character vector of group labels excluded by
\code{exclude_below_min} or \code{fully_crossed}, or \code{NULL} if no exclusions.}
\item{excluded_values}{Named list of value_selection filters applied,
or \code{NULL} if none.}
}
}
\description{
The main entry point for \code{iDIFr}. Detects Differential Item Functioning (DIF)
using one or more statistical methods, with full support for intersectional
group structures defined by crossing multiple demographic variables.

Effect sizes are reported alongside significance for all methods. Groups with
small cell sizes trigger a warning. Use \code{exclude_below_min} and
\code{fully_crossed} to control whether those groups are included in the analysis.
}
\examples{
\dontrun{
# Basic two-group analysis
result <- idifr(
  data   = my_data,
  items  = 1:20,
  group  = ~ gender,
  method = c("LR", "LRT")
)

# Intersectional analysis, excluding groups below minimum
result <- idifr(
  data              = my_data,
  items             = 1:20,
  group             = ~ gender * nationality * age_band,
  method            = c("LR", "LRT"),
  min_cell_size     = 50,
  exclude_below_min = TRUE
)

# Only include nationalities fully crossed across gender and age_band
result <- idifr(
  data          = my_data,
  items         = 1:20,
  group         = ~ gender * nationality * age_band,
  method        = "LR",
  fully_crossed = "nationality"
)

# Restrict to specific variable values
result <- idifr(
  data            = my_data,
  items           = 1:20,
  group           = ~ gender * nationality * age_band,
  method          = "LR",
  value_selection = list(nationality = c("UK", "France"),
                         age_band    = c("Young", "Old"))
)
}

}
\seealso{
\code{\link[=check_groups]{check_groups()}} for exploring group structure before analysis;
\code{\link[=group_details]{group_details()}} and \code{\link[=cross_details]{cross_details()}} for full breakdowns;
\code{\link[=merge_groups]{merge_groups()}} for combining sparse cells.
}
