% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_results}
\alias{export_results}
\title{Export iDIFr results to Excel}
\usage{
export_results(x, file, sheets = NULL, overwrite = TRUE)
}
\arguments{
\item{x}{An \code{idifr} object returned by \code{\link[=idifr]{idifr()}}.}

\item{file}{Path to the output \code{.xlsx} file (character string).}

\item{sheets}{Character vector of sheet keys to include.  Valid keys:
\code{"summary"}, \code{"lr"}, \code{"lrt"}, \code{"mob"}, \code{"direction"}, \code{"ica"},
\code{"groups"}.  Pass \code{NULL} (default) to include all available sheets.}

\item{overwrite}{Logical.  If \code{TRUE} (default) an existing file is
silently overwritten.}
}
\value{
\code{x} invisibly (so the call can be piped).
}
\description{
Writes an \code{idifr} result object to a formatted \code{.xlsx} workbook.  Each
requested sheet is written as an Excel table so that column headers are
bold, filters are enabled, and values are properly typed.

Only columns that are actually present in the result object are written;
columns listed in the per-method definitions that were not produced by the
current run are silently omitted.
}
\examples{
\donttest{
if (requireNamespace("openxlsx", quietly = TRUE)) {
  dat <- simulate_dif(300, 10, dif_items = c(3, 7), seed = 1)
  result <- idifr(dat, 1:10, ~ group, method = "LR", verbose = FALSE)
  export_results(result, tempfile(fileext = ".xlsx"))
}
}

}
